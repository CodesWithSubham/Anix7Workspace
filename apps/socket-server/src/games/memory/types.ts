// export type GameState = {
//   id: string;
//   tgId: string;
//   board: string[]; // shuffled cards (pairs)
//   revealed: boolean[]; // which cards are visible
//   matched: boolean[]; // which pairs are matched
//   lastPick: number | null;
//   moves: number;
//   createdAt: number;
// };
